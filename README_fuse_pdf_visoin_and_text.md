## How to run:

```python
python fuse_pdf_vision_and_text.py \
  --input outputs/json_file.json \
  --out-dir output/fused_qwen3 \
  --no-per-ko-files
```

## Output will be written to:
```
outputs/<input_stem>_visioned.json
```